#include <iostream>
#include <vector>

int main() {
    // cars is the variable name of an array with two elements that are strings.
    std::vector<std::string> myStringVector = {"Hello", "World", "C++", "Vectors"};
    std::vector<std::string> cars = {"hello" , "friend"};
    std::cout << cars[1];
    return 0;
}
